import SwiftUI

struct LoadingView: View {
    var body: some View {
        VStack {
            Spacer()
            
            Text("Pay Attention Club")
                .font(.system(size: 36, weight: .bold))
                .padding()
            
            Spacer()
        }
    }
}

